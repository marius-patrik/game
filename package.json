{
  "name": "game",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:client": "bun run --filter @game/client dev",
    "dev:server": "bun run --filter @game/server dev",
    "build": "bun run --filter '*' build",
    "build:release": "bun run --filter @game/client build && bun apps/server/scripts/generate-static.ts && bun apps/server/scripts/generate-migrations.ts && NODE_ENV=production bun build --compile --minify --sourcemap --define process.env.NODE_ENV='\"production\"' apps/server/src/index.ts --outfile dist/game-server",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "typescript": "^5.7.2"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
